import { CriarPaisMemoriaRepository } from './criar-pais.memoria.repository';

describe('CriarPaisMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new CriarPaisMemoriaRepository()).toBeDefined();
  });
});

import { EncontrarEstadoPeloNomeMemoriaRepository } from './encontrar-estado-pelo-nome.memoria.repository';

describe('EncontrarEstadoPeloNomeMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarEstadoPeloNomeMemoriaRepository()).toBeDefined();
  });
});

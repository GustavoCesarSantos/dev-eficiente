import { EncontrarEstadoPeloIdMemoriaRepository } from './encontrar-estado-pelo-id.memoria.repository';

describe('EncontrarEstadoPeloIdMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarEstadoPeloIdMemoriaRepository()).toBeDefined();
  });
});

import { EncontrarPaisPeloNomeMemoriaRepository } from './encontrar-pais-pelo-nome.memoria.repository';

describe('EncontrarPaisPeloNomeMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarPaisPeloNomeMemoriaRepository()).toBeDefined();
  });
});

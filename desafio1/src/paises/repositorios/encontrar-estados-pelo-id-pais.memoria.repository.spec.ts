import { EncontrarEstadosPeloIdPaisMemoriaRepository } from './encontrar-estados-pelo-id-pais.memoria.repository';

describe('EncontrarEstadosPeloIdPaisMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarEstadosPeloIdPaisMemoriaRepository()).toBeDefined();
  });
});

import { CriarAutorMemoriaRepository } from './criar-autor-memoria.repository';

describe('CriarAutorMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new CriarAutorMemoriaRepository()).toBeDefined();
  });
});

import { EncontrarAutorMemoriaRepository } from './encontrar-autor-memoria.repository';

describe('EncontrarAutorMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarAutorMemoriaRepository()).toBeDefined();
  });
});

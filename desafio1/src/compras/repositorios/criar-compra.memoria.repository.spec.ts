import { CriarCompraMemoriaRepository } from './criar-compra.memoria.repository';

describe('CriarCompraMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new CriarCompraMemoriaRepository()).toBeDefined();
  });
});

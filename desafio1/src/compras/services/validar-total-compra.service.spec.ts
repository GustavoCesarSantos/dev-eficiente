import { ValidarTotalCompraService } from './validar-total-compra.service';

describe('ValidarTotalCompraService', () => {
  it('should be defined', () => {
    expect(new ValidarTotalCompraService()).toBeDefined();
  });
});

import { PegarTotalSemDescontoService } from './pegar-total-sem-desconto.service';

describe('PegarTotalSemDescontoService', () => {
  it('should be defined', () => {
    expect(new PegarTotalSemDescontoService()).toBeDefined();
  });
});

import { EncontrarLivroPeloTituloMemoriaRepository } from './encontrar-livro-pelo-titulo-memoria.repository';

describe('EncontrarLivroPeloTituloMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarLivroPeloTituloMemoriaRepository()).toBeDefined();
  });
});

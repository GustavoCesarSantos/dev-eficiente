import { EncontrarLivroMemoriaRepository } from './encontrar-livro-memoria.repository';

describe('EncontrarLivroMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new EncontrarLivroMemoriaRepository()).toBeDefined();
  });
});

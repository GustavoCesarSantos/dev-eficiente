import { ListarLivrosMemoriaRepository } from './listar-livros-memoria.repository';

describe('ListarLivrosMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new ListarLivrosMemoriaRepository()).toBeDefined();
  });
});

import { ListarLivrosPelosIdsMemoriaRepository } from './listar-livros-pelos-ids-memoria.repository';

describe('ListarLivrosPelosIdsMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new ListarLivrosPelosIdsMemoriaRepository()).toBeDefined();
  });
});

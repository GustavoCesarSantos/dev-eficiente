import { CriarCupomDeDescontoMemoriaRepository } from './criar-cupom-de-desconto-memoria.repository';

describe('CriarCupomDeDescontoMemoriaRepository', () => {
  it('should be defined', () => {
    expect(new CriarCupomDeDescontoMemoriaRepository()).toBeDefined();
  });
});
